#pragma once

#include <ostream>
#include <Mesh.h>
#include <iomanip>
#include <Texture.h>

// inline std::ostream& operator << (std::ostream& out, const UV& a) {
//     out << std::setprecision(5) << std::fixed << "(u: " << a.u << ", v: " << a.v << ")";
//     return out;
// }

// inline void dumpMatrix(const GLfloat m[16], std::ostream& out) {
//     for (int y = 0; y < 4; y++)
//         out << std::setprecision(5) << std::fixed << m[y * 4 + 0] << ", " << m[y * 4 + 1] << ", " << m[y * 4 + 2] << ", " << m[y * 4 + 3] << std::endl;
// }


// void drawNormals(const Mesh& mesh);
