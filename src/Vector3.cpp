#include <Vector3.h>

// Vector3 operator - (const Vector3& first, const Vector3& second) {
//     return {
//         first.x - second.x,
//         first.y - second.y,
//         first.z - second.z
//     };
// }

// Vector3 operator + (const Vector3& first, const Vector3& second) {
//     return {
//         first.x + second.x,
//         first.y + second.y,
//         first.z + second.z
//     };
// }



// Vector3 operator * (const Vector3& a, double amount) {
//     return {
//         a.x * amount,
//         a.y * amount,
//         a.z* amount
//     };
// }

// Vector3 operator / (const Vector3& a, double amount) {
//     return {
//         a.x / amount,
//         a.y / amount,
//         a.z / amount
//     };
// }

// Vector3 cross(const Vector3& first, const Vector3& second) {
//     return {
//         first.y* second.z - first.z * second.y,
//         first.z* second.x - first.x * second.z,
//         first.x* second.y - first.y * second.x,
//     };
// }

// double length(const Vector3& a) {
//     return sqrt(a.x * a.x +
//                 a.y * a.y +
//                 a.z * a.z);
// }

// Vector3 normalize(const Vector3& a) {
//     double len = length(a);
//     if (len > 0.0000001)
//         return a / len;
//     else
//         return a;
// }

